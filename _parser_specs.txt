All three emit Msg[] = {author:string, text:string, ts:number /*epoch ms*/, kind:'text'|'media'|'voice'|'system'}, matching api/src/engine/types.ts. CRITICAL INTEGRATION RULE (verified from personas.service.ts L295-312): the two-phase author-picker counts authors over EVERY emitted message — it does NOT filter kind:'system'. The 3 existing parsers therefore never emit 'system' rows; they `continue` past them. NEW PARSERS MUST DO THE SAME: drop system events entirely (do not push kind:'system') so they don't pollute the {authors:[{name,count}]} list or mislead the top-non-'me' personaAuthor pick. author MUST be a stable human-readable DISPLAY NAME (the picker shows it to the user and matches `me` against it by string equality). Each parser is a pure (content:string)=>Msg[] function in api/src/engine/parsers/<name>.ts mirroring the existing trio (export parse<Name>String for the service, plus a thin parse<Name>(path) using readFileSync). REGISTRATION (4 touch-points, all in scope): (a) add to PARSERS map + Source union in api/src/personas/personas.service.ts; (b) add to @IsIn([...]) and the source?: union in api/src/personas/dto.ts; (c) add the source id to the frontend wizard source list; (d) reuse the SHARED mojibake helper — extract instagram.ts fixMojibake() into engine/parsers/_encoding.ts and import it (FB uses it; LINE/VK must NOT).\n\n=== 1) facebook-messenger (source id: 'facebook') ===\nINPUT FILE(S): your_facebook_activity/messages/inbox/<THREAD>/message_1.json plus message_2.json…message_N.json for big threads (older exports: top-level messages/inbox/<THREAD>/). Mobile users upload the ZIP; the ingest layer must glob every message_*.json under each inbox/<THREAD>/, JSON.parse each, and MERGE their `messages` arrays (each file repeats the same top-level {participants,messages,title,...}). The parse<Facebook>String contract takes ONE file's content; add a small pre-step (in storage/upload handling) that concatenates merged messages into a single synthetic {participants, messages} object before calling the parser, OR have the parser accept an already-merged object. Dedupe by (timestamp_ms, sender_name, content) since split boundaries can overlap.\nPARSE: JSON.parse FIRST (file is valid JSON with \\u00XX escapes), THEN apply mojibake fix per-string. TOP-LEVEL: participants:[{name}], messages:[{...}], title. MESSAGE keys: sender_name(str), timestamp_ms(int), content(str, optional), type(str), is_unsent(bool?), photos[]/videos[]/audio_files[]/gifs[]/files[]/sticker{}/share{} (optional), reactions[], call_duration(int?).\nMAP: author = fixMojibake(sender_name); text = content ? fixMojibake(content).trim() : ''. ts = timestamp_ms (ALREADY epoch MILLISECONDS — use directly, do NOT *1000; note attachment creation_timestamp is SECONDS but we don't use it for ts). kind: audio_files?.length => 'voice'; else photos||videos||gifs||sticker||files||share => 'media'; else if content => 'text'. DROP (do not emit): is_unsent===true; call_duration present OR type 'Call'/'call'; type in {Subscribe,Unsubscribe,Plan,Payment}; content matching /You are now connected on Messenger|sent an attachment\\.|started a call|named the group|added|removed|changed the/i; and pure-text rows with empty text after trim (matches existing Instagram parser's `if(kind==='text'&&!text)continue`). For media/voice rows set text='' (mirror WhatsApp parser).\nENCODING: REUSE fixMojibake (Buffer.from(s,'latin1').toString('utf8')) on content, sender_name, title, participants[].name, reactions[].reaction — byte-identical to Instagram. Cyrillic fully recovered.\nORDERING: messages are NEWEST-first within and across message_N files; after merge sort ASCENDING by timestamp_ms (segment() re-sorts too, but do it here for dedupe sanity).\nE2EE: standard DYI silently omits end-to-end-encrypted threads — feature-flag a separate 'facebook-e2ee' source later for the desktop secure-storage messages.zip (similar schema, verify keys against a real file before trusting). Not in this lane.\nAUTHOR-PICKER COMPAT: emits exactly the participant display names (≤ a handful), perfect for the picker. Self detected when user picks `me`=their own fixMojibake(name) from participants.\n\n=== 2) line (source id: 'line') ===\nINPUT FILE: single .txt, UTF-8, named '[LINE] Chat history with <Name>.txt'. Read as utf8, strip a leading BOM (\\uFEFF) if present. DO NOT apply fixMojibake (would corrupt clean UTF-8).\nPARSE (state machine over lines):\n- PREAMBLE: skip leading lines until first date-section header. (Title line '[LINE] Chat history with …' + 'Saved on: …'.)\n- DATE SECTION HEADER: /^(\\d{4})\\.(\\d{2})\\.(\\d{2})\\s+\\S+$/ (e.g. '2017.09.03 Sunday', no TAB) — sets current y/m/d context.\n- MESSAGE LINE: detect /^(\\d{1,2}:\\d{2})(\\s?[AP]M)?\\t/ . Split on TABs: parts=line.split('\\t'); time=parts[0]; author=parts[1]; text=parts.slice(2).join('\\t'). NEVER split on spaces (display names contain spaces). Support both 24h 'HH:MM' and 12h 'h:mm AM/PM' (try 24h, fall back to meridiem).\n- CONTINUATION: any line that is neither a date header nor a message line is appended to the previous message's text with '\\n' (same as our WhatsApp multi-line handling). Preserve blank lines inside a message.\nTIMESTAMP→ms: ts = new Date(y, m-1, d, hh, mm).getTime() (LOCAL device time, no offset in file — same caveat as WhatsApp/Telegram-desktop; optionally let user pick TZ later).\nKIND: match author-field/text against known LINE placeholder set (case-insensitive): '[Photo]','[Sticker]','[Video]','[File]','[Voice message]','[Contact]','[Album]','[Location]' => 'media' (default any unknown [bracketed] token to 'media'); call/unsent/join lines ('☎ Call time …','Missed call','Canceled call','You unsent a message','X unsent a message', invites/name-changes) => DROP (these are system; many have NO author column, i.e. only 2 tab-fields). LINE has no separable voice-note placeholder distinct from calls → treat calls as system (drop); '[Voice message]' stays 'media'. else 'text'. For media set text=''; for text trim and `if(!text)continue`.\nMEDIA/VOICE COUNT: stats count media via kind; LINE rarely yields kind:'voice' — acceptable.\nDESKTOP EXPORT GUARD: Windows 'Save chat' is SPACE-delimited not TAB — if NO message line in the file contains a TAB, reject with the existing 'Could not read the file' UnprocessableEntity (don't attempt heuristic space-parse).\nAUTHOR-PICKER COMPAT: authors are the LINE display names (own messages appear under the exporter's own display name, NOT literally 'You') — exactly what the picker needs; user selects which name is `me`.\n\n=== 3) vk (source id: 'vk') ===\nINPUT: unzipped messages/ folder from the VK archive (or the whole .zip). Files are HTML, encoding windows-1251 (CP1251), single-pass — NOT the Instagram double-encode. Add a SEPARATE decode branch: read each file as a Buffer and decode with iconv-lite 'win1251' (or TextDecoder('windows-1251')); then HTML-entity-decode (&amp; etc.). DO NOT reuse fixMojibake (would corrupt). The parse<Vk>String contract takes ALREADY-DECODED HTML string for ONE page file; the upload/ingest layer iterates dialogs and pages and concatenates results.\nDISCOVER DIALOGS: iterate messages/ subdirs; folder name = numeric peerId (positive=user DM, negative=community, 2e9+=group chat). Within a dialog, sort page files by the int in /messages(\\d+)\\.html/ ascending (0,50,100… = chronological).\nPER MESSAGE: select each div.message. HEADER div.message__header textContent = 'Name, D MMM YYYY в H:MM:SS'. Split on LAST comma (lastIndexOf(',')): left=author display, right=timestamp string. If author text === 'Вы' it's the archive OWNER (self) → use a consistent self display name (resolve from the archive owner / let user pick); profile link = first <a> inside header (href https://vk.com/idNNN|/clubNNN|vanity) — use id as stable key if present, else display name.\nTIMESTAMP→ms: parse with /^(\\d{1,2})\\s+([а-я]{3})\\s+(\\d{4})\\s+в\\s+(\\d{1,2}):(\\d{2}):(\\d{2})$/i and the NON-STANDARD genitive month map {янв:1,фев:2,мар:3,апр:4,мая:5,июн:6,июл:7,авг:8,сен:9,окт:10,ноя:11,дек:12} (note 'мая' not 'май'). VK times are MOSCOW (UTC+3) with NO offset → build the date as UTC components then SUBTRACT 3h: ts = Date.UTC(y,m-1,d,hh-3,mm,ss) (Date.UTC normalizes the -3 rollover). EDITED: header has <span title='D MMM YYYY в H:MM:SS'>(ред.)</span> — strip '(ред.)' before parsing the main date; ignore the edit time for ts.\nBODY: the content div is the sibling AFTER div.message__header; remove nested div.kludges first; replace <br> with '\\n'; decode VK mention markup /\\[([^|\\]]+)\\|([^\\]]+)\\]/ → keep group2 as display text; HTML-entity-decode.\nKIND: SYSTEM (div.kludges containing a.im_srv_lnk / b.im_srv_lnk → created/renamed chat, pinned, kicked/invited, left, photo change, call, screenshot) => DROP (do not emit, to keep author counts clean). MEDIA/VOICE via div.attachment > div.attachment__description localized type: 'Голосовое сообщение' OR an a.attachment__link href matching /audiomsg\\/.+\\.ogg/ => 'voice'; 'Фотография'/'Видеозапись'/'Аудиозапись'/'Документ'/'Стикер'/'Запись со стены'/'Ссылка' => 'media'. else 'text'. For media/voice set text='' (or keep a short placeholder — but '' matches our other parsers); for text, `if(!text)continue`.\nAUTHOR-PICKER COMPAT: authors = VK display names (others) + the resolved owner name for 'Вы'. To make 'Вы' selectable as `me`, map it to a concrete display name (the dialog header's owner crumb, or simply the literal owner name) BEFORE counting — the picker matches `me` by string equality, so 'Вы' must be normalized to one stable label across all dialogs. Recommended: resolve owner name once from index-messages.html / archive metadata and substitute it for every 'Вы'.\nLARGE FILES: stream per page file; archives are big.\n\nSHARED: extract fixMojibake into engine/parsers/_encoding.ts; FB imports it, LINE/VK explicitly do not. Add iconv-lite as a dep for VK. All three keep the existing `if(kind==='text'&&!text)continue` and media/voice text='' conventions so stats.ts (counts kind:'voice'/'media') and segment.ts (renders [media]/[voice]) work unchanged.